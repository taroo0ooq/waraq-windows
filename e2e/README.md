# e2e / QA automation — Waraq Windows (WRQ-WIN-002)

**Owner desk:** Stagecraft QA  
**Stack:** WinUI 3 unpackaged under `src/` + WorkerW host

## What runs in CI

Workflow: [`.github/workflows/playwright.yml`](../.github/workflows/playwright.yml) (required check name: **playwright**)

| Job | Runner | Purpose |
|-----|--------|---------|
| **Windows QA smoke** | `windows-latest` | Restore + build tests + `dotnet test` + WinUI App x64 build |
| **Browser Playwright N/A** | `ubuntu-latest` | Explicit N/A — no browser UI |

Local full smoke (preferred for Phase 3-QA evidence):

```powershell
# From repo root (Windows + .NET 8; ffmpeg optional for fixtures)
./e2e/windows-qa-smoke.ps1 -Configuration Release
```

## Fixtures

`e2e/fixtures/` (generated by smoke if missing):

- `sample.gif` — tiny animated GIF  
- `sample.mp4` — 1s color video  

## Automated coverage

| Layer | Location | Covers |
|-------|----------|--------|
| Unit | `src/Waraq.Windows.Tests` | Path gate (UNC/URL/size), classifier, Phase3 playable kinds, WorkerW probe, virtual screen |
| Smoke script | `e2e/windows-qa-smoke.ps1` | Build + test + 4s app launch |
| CI playwright | `.github/workflows/playwright.yml` | Same core build/test/app on GHA |

## Manual / soft residual

1. Interactive Browse file picker + visual wallpaper judgment (owner L&F parallel)
2. Tray context menu **Stop wallpaper** click (wired to `App.Wallpaper.Stop` — code path verified)
3. FlaUI/WinAppDriver full UI drive (deferred)
4. Browser Playwright specs (N/A until web surface)

Matrix: [`docs/qa/WRQ-WIN-002-phase3-qa-matrix.md`](../docs/qa/WRQ-WIN-002-phase3-qa-matrix.md)
