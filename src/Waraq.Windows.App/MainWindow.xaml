<Window
    x:Class="Waraq.Windows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Settings">

    <!-- Phase 2 Settings shell: NavigationView ~190dip sidebar + stub panes (DESIGN.md) -->
    <Grid>
        <NavigationView
            x:Name="NavView"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            OpenPaneLength="190"
            CompactPaneLength="48"
            IsPaneToggleButtonVisible="False"
            PaneDisplayMode="Left"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.PaneFooter>
                <Grid Padding="12,10" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
                        <TextBlock Text="Advanced" FontWeight="SemiBold" FontSize="12"/>
                        <TextBlock Text="Full depth controls" FontSize="10" Opacity="0.7"/>
                    </StackPanel>
                    <ToggleSwitch x:Name="AdvancedToggle"
                                  Grid.Column="1"
                                  OffContent=""
                                  OnContent=""
                                  MinWidth="0"
                                  Toggled="AdvancedToggle_Toggled"/>
                </Grid>
            </NavigationView.PaneFooter>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Frame x:Name="ContentFrame" Grid.Row="0"/>

                <TextBlock x:Name="FooterStatus"
                           Grid.Row="1"
                           Margin="28,8,28,12"
                           FontSize="11"
                           Opacity="0.65"
                           TextWrapping="Wrap"/>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
