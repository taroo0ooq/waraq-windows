<Window
    x:Class="Waraq.Windows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Waraq for Windows">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="1"
                   Margin="0,12,0,0"
                   Opacity="0.8"
                   TextWrapping="Wrap"/>

        <TextBlock Grid.Row="2"
                   Margin="0,20,0,8"
                   Style="{StaticResource BodyStrongTextBlockStyle}"
                   Text="Planned Settings panes (Phase 2)"/>

        <ListView x:Name="PanesList"
                  Grid.Row="3"
                  SelectionMode="None"
                  IsItemClickEnabled="False"/>

        <TextBlock x:Name="ProbeText"
                   Grid.Row="4"
                   Margin="0,16,0,0"
                   Opacity="0.7"
                   TextWrapping="Wrap"
                   FontSize="12"/>
    </Grid>
</Window>
