<Window x:Class="Waraq.Windows.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Waraq for Windows"
        Height="520"
        Width="640"
        MinHeight="440"
        MinWidth="520"
        WindowStartupLocation="CenterScreen"
        Background="#FF121212">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProductTitle"
                   Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="#FFF5F5F5"
                   Text="Waraq for Windows"/>

        <TextBlock x:Name="VersionText"
                   Grid.Row="1"
                   Margin="0,8,0,0"
                   Foreground="#FFB0B0B0"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="StrategyText"
                   Grid.Row="2"
                   Margin="0,8,0,0"
                   Foreground="#FF9E9E9E"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <TextBlock Text="Media file (video or GIF)" Foreground="#FFCFCFCF" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox"
                         Grid.Column="0"
                         Height="30"
                         VerticalContentAlignment="Center"
                         Background="#FF1E1E1E"
                         Foreground="#FFE0E0E0"
                         BorderBrush="#FF444444"
                         Padding="8,0"/>
                <Button Grid.Column="1"
                        Margin="8,0,0,0"
                        Padding="12,4"
                        Content="Browse…"
                        Click="OnBrowseClick"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,12,0,0" Orientation="Horizontal">
            <TextBlock Text="Fit" Foreground="#FFCFCFCF" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="FitBox"
                      Width="160"
                      SelectedIndex="0">
                <ComboBoxItem Content="Fill (cover)"/>
                <ComboBoxItem Content="Stretch"/>
                <ComboBoxItem Content="Fit (letterbox)"/>
            </ComboBox>
        </StackPanel>

        <Border Grid.Row="5"
                Margin="0,16,0,0"
                Padding="12"
                Background="#FF1E1E1E"
                CornerRadius="6"
                BorderBrush="#FF333333"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="StatusText"
                           Foreground="#FFCFCFCF"
                           TextWrapping="Wrap"
                           Text="Pick a local .mp4/.webm/.gif (etc.), then Apply wallpaper."/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="6" Margin="0,16,0,0" Orientation="Horizontal">
            <Button Content="Apply wallpaper" Padding="16,8" Margin="0,0,8,0" Click="OnApplyClick"/>
            <Button Content="Stop" Padding="16,8" Margin="0,0,8,0" Click="OnStopClick"/>
            <Button Content="Probe host" Padding="16,8" Click="OnProbeClick"/>
        </StackPanel>
    </Grid>
</Window>
